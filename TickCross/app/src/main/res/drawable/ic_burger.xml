<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="120dp"
    android:height="120dp"
    android:viewportWidth="@integer/viewport_width"
    android:viewportHeight="@integer/viewport_height">

    <group
        android:name="@string/groupBurgerArrow"
        android:pivotX="@integer/viewport_center_x_ba"
        android:pivotY="@integer/viewport_center_y_ba">

        <path
            android:name="@string/burger"
            android:pathData="@string/path_burger"
            android:strokeWidth="@integer/stroke_width"
            android:strokeLineCap="square"
            android:strokeColor="@color/stroke_color"/>

    </group>

</vector>